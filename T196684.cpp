#include<iostream>
#include<cstring>

using namespace std;
string mx[50002],mi[50002];
int mxn,mim;
void read(string s){
    cin>>s;
    s.erase(0,1);
    if(s[0]=='>'){
        if(s[1]=='='){
            
        }
    }
    else{}
}
int main(){
    string maxn,minn;
    bool ax,in;
    string ss;
    read(ss); 
    return 0;
}